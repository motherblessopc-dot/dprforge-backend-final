# DPRForge backend — minimal runtime dependencies for Railway / production
# (No dev tools like pytest, mypy, flake8 here — keeps build fast and reliable.)

# --- Web framework ---
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-multipart>=0.0.9

# --- Config / env ---
python-dotenv>=1.0.1

# --- MongoDB ---
motor==3.3.1
pymongo==4.5.0

# --- Pydantic + validators ---
pydantic>=2.6.4
email-validator>=2.2.0

# --- Auth ---
pyjwt>=2.10.1
bcrypt==4.1.3

# --- HTTP client (used by /auth/google-session call to Emergent auth) ---
requests>=2.31.0

# --- PDF / Excel generation ---
reportlab>=4.0.9
openpyxl>=3.1.5
Pillow>=10.0.0

# --- QR codes (UPI payment QR) ---
qrcode[pil]>=7.4.2

# --- Emergent LLM (AI narrative). OPTIONAL — uncomment ONLY if you want
#     the "Generate AI narrative" button to work. The rest of the app runs
#     fine without it (server.py imports this with try/except).
#     This package is on a custom index — if your Railway build fails because
#     of this line, just keep it commented out.
# emergentintegrations>=0.1.0
razorpay==2.0.1
